{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Schema for CMS service request. See Decrypt function on pg 225 in CMS spec for more detail of contents",
  "type":"object",
  "required":[
    "associatedData",
    "cipherText",
    "startingVariable"
  ],
  "properties":{
    "associatedData":{
      "description":"Associated data in format 'Sender_ID; ServiceRequest_ID; Service_ID;', See pg 128 in CMS spec for more info. Sender_ID is used to identify CCM key.",
      "type":"string"
    },
    "cipherText":{
      "description":"Data authenticated and encrypted",
      "type":"string"
    },
    "startingVariable":{
      "description":"Starting Variable (nonce)",
      "type":"string"
    }
  }
}